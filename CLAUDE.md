# Open Munch Game

Карточная игра «Манчкин» — Angular 21 + TypeScript 5.9 + Vitest

## Команды

```bash
npm start        # dev-сервер (ng serve)
npm run build    # production-сборка
npm test         # запуск тестов (vitest)
```

## Архитектура

- **Zoneless** — `provideZonelessChangeDetection()` в `app.config.ts`
- **OnPush** — все компоненты используют `ChangeDetectionStrategy.OnPush`
- **Standalone** — все компоненты standalone (Angular 21 по умолчанию)
- **Signals** — состояние игры на Angular signals, без RxJS где возможно

## Структура

```
src/app/
  core/models/    — типы и интерфейсы (карты, игроки, состояние)
  core/engine/    — сервисы игрового движка (колоды, бой, ходы, экипировка)
  core/bot/       — AI ботов
  features/       — UI-компоненты (lobby, game-board, hand, combat, card)
  shared/         — общие утилиты
  data/           — данные карт
```

## Конвенции

- Минимум внешних зависимостей
- CSS только для позиционирования, базовые HTML-элементы
- Тесты рядом с файлами: `*.spec.ts`
- Цель покрытия: 80%

---

## Дорожная карта разработки

### Этап 1 — Фундамент (модели, данные, базовый движок)

- [x] **Модели карт** — `card.model.ts`: типы `MonsterCard`, `CurseCard`, `ClassCard`, `RaceCard`, `EquipmentCard`, `OneShotCard`, `LevelUpCard`, union-типы `DoorCard`, `TreasureCard`, `Card`
- [x] **Модель игрока** — `player.model.ts`: `Player`, `PlayerEquipment`, `createPlayer()`, `getCombatStrength()`, `getEquipmentBonus()`
- [x] **Модель состояния игры** — `game-state.model.ts`: `GameState`, `CombatState`, `TurnPhase`, `getCurrentPlayer()`
- [x] **Данные карт дверей** — `door-cards.ts`: 15 монстров, 5 проклятий, 4 класса, 3 расы (26 карт)
- [x] **Данные карт сокровищ** — `treasure-cards.ts`: 15 экипировки, 5 одноразовых, 3 уровня (23 карты)
- [x] **Сервис колод** — `deck.service.ts`: создание, тасование (Fisher-Yates), вытягивание, сброс, авто-перетасовка
- [x] **Тесты колод** — `deck.service.spec.ts` (23 теста)

### Этап 2 — Игровой движок

- [x] **Сервис экипировки** — `equipment.service.ts`: `canEquip()`, `equip()`, `unequipSlot()`, ограничения по классу/расе, двуручное оружие
- [x] **Тесты экипировки** — `equipment.service.spec.ts` (16 тестов)
- [x] **Сервис состояния игры** — `game-state.service.ts`: `startGame()`, `kickDoor()`, `resolveCombat()`, `runAway()`, `endTurn()`, `playCardFromHand()`, `useOneShotInCombat()`, `lootRoom()`
- [x] **Тесты состояния** — `game-state.service.spec.ts`
- [x] **Интеграция экипировки в GameStateService** — обновление экипировки игрока через `equip()`/`unequip()` в состоянии, боты экипируют через `playCardFromHand()`
- [x] **Полная реализация проклятий** — все типы `CurseEffect` (lose-equipment, lose-hand, lose-class, lose-race) с корректной обработкой, карты уходят в discard
- [x] **Bad stuff монстров** — структурированный `BadStuffEffect` на модели `MonsterCard`, обработка lose-levels, lose-equipment-slot, lose-all-equipment, lose-hand, skip-turn
- [ ] **Система помощника в бою** — `CombatState.helper` определён, но не используется
- [x] **Лимит карт на руке** — сброс лишних карт в фазе charity (6 для Дварфа, 5 для остальных), UI для сброса, боты сбрасывают автоматически
- [x] **Продажа карт / золото** — обмен 1000 золота на уровень, UI для продажи в фазе charity

### Этап 3 — AI ботов

- [x] **Сервис ботов** — `bot.service.ts`: `playBotTurn()`, стратегии экипировки, разыгрывания карт, использования одноразовых в бою
- [x] **Тесты ботов** — `bot.service.spec.ts`
- [ ] **Улучшение AI** — учёт силы монстра, решение о побеге, выбор лучшей экипировки с учётом бонусов
- [ ] **AI помощь / вредительство** — бот решает, помогать ли другим в бою или мешать

### Этап 4 — UI-компоненты

- [x] **CardComponent** — отображение карты с типо-зависимой информацией, русская локализация
- [x] **HandComponent** — рука игрока, отображение карт, событие выбора
- [x] **CombatComponent** — UI боя: расчёт силы, бонусы, действия (бой/побег/предмет)
- [x] **LobbyComponent** — экран настройки игры, выбор кол-ва ботов (3–6)
- [x] **GameBoardComponent** — главный экран: панель игроков, ход, бой, рука, экипировка, лог
- [x] **Тесты компонентов** — `card.component.spec.ts`, `lobby.component.spec.ts`

### Этап 5 — Роутинг, навигация и деплой

- [x] **Конфигурация приложения** — `app.config.ts`: zoneless, router
- [x] **Корневой компонент** — `app.ts`: `<router-outlet />`
- [x] **Определение маршрутов** — `app.routes.ts`: `/` → Lobby, `/game` → GameBoard
- [x] **GitHub Pages деплой** — GitHub Actions workflow для автоматической сборки и публикации на GitHub Pages

### Этап 6 — Особые способности классов и рас

- [ ] **Способности классов** — Воин (берсерк), Волшебник (чары), Вор (удар в спину), Клирик (изгнание нежити)
- [ ] **Способности рас** — Эльф (+1 уровень за победу), Дварф (6 карт на руке), Халфлинг (продажа за двойную цену)
- [ ] **Тесты способностей** — покрытие тестами для каждой способности

### Этап 7 — Расширение контента

- [ ] **Больше монстров** — расширить набор монстров (сейчас 15)
- [ ] **Больше экипировки** — добавить карты экипировки с уникальными эффектами
- [ ] **Больше проклятий** — разнообразные эффекты проклятий
- [ ] **Больше одноразовых карт** — поддержка карт помех для других игроков

### Этап 8 — Полировка и UX

- [ ] **Стилизация карт** — визуальное оформление карт по типам (цвета, иконки)
- [ ] **Анимации** — переходы между фазами, эффекты боя, бросок кубика
- [ ] **Лог игры** — улучшенный лог с прокруткой и фильтрацией
- [ ] **Адаптивная вёрстка** — ориентация на мобильные устройства (~375px), поддержка десктопа
- [ ] **Звуки** — звуковые эффекты для ключевых действий

---

## Известные проблемы

- `shared/` папка пустая / не создана
- Система помощника в бою не реализована (`CombatState.helperId`/`helperBonuses` определены, но не используются)
